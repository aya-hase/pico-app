# Pico AI Companion App - Phase 2 (Live Version) Walkthrough

実物のデータベース（Supabase）およびAI API（Gemini）と接続した、本格稼働版の「ピコ」がローカルサーバーで起動しました！
以前ブラウザで発生していた互換性エラー（Type error）も解決されています。

---

## 主な追加・変更内容

1. **Supabaseとの連携（DB & 認証）**:
   - メールアドレスとパスワードによるアカウント作成・ログインが、本物の **Supabase Auth** に接続されました。
   - アカウント新規登録時、自動でデータベースにプロフィールレコード（selected_character = 'clara'）が挿入されます。
   - 会話履歴 (`chats`)、日記 (`diaries`)、カレンダー予定 (`schedules`) のすべてが、Supabase上の各テーブルにリアルタイムで保存・取得されます。
2. **Gemini API（AI）との接続**:
   - 会話の返答作成に `gemini-1.5-flash` モデルを接続しました。
   - 各キャラクター（くらら、まろ、フレデリカ）の個性ある口調と、過度な励ましを控える「おためごかし防止ルール」をシステムプロンプトに組み込みました。
3. **カレンダー自動連携＆フォロー機能（アプリ内カレンダー）**:
   - 会話の中で予定（例：「明日15時にミーティング」「金曜日デートする」など）を話すと、Geminiが自動的に予定情報を抽出し、Supabaseの `schedules` テーブルへ自動保存します。
   - 予定日当日になると、ホーム画面に「今日の予定」が通知されます。
   - その状態でチャットを開始すると、AIが「今日はミーティングだったね！どうだった？」と能動的に問いかけてくれます。
4. **音声入力機能（Web Speech API）**:
   - チャットの入力欄にあるマイクボタンを押すと、声で喋った内容がリアルタイムに文字起こしされ、入力欄に挿入されます。
5. **入力中インジケータ**:
   - AIが考えたり返答を生成している間、アバターの横に「入力中（ポコポコと動くドットアニメーション）」が表示され、自然な会話のタメを感じられます。
6. **過去の発言キャラアバターの出し分け**:
   - 着せ替えによって現在のキャラを変更しても、過去に送信されたメッセージには「当時話していたキャラクター」のアバター画像が表示されます。

---

## 動作確認（テスト）の手順

デベロップメントサーバーが起動しています。ブラウザで以下のURLを開いてください。

👉 **[http://localhost:3000](http://localhost:3000)**

### 💡 1. ログインと会員登録
1. 新しくテスト用アカウントを作成するため、画面下の「**新規登録はこちら**」を押します。
2. 「お名前」「メールアドレス」「パスワード」を入力して「**アカウント登録してはじめる**」を押します。
   - ※Supabaseのダッシュボードでユーザーと `profiles` テーブルにレコードが登録されることを確認できます。

### 💡 2. 音声入力とおしゃべり
1. チャット画面を開きます。
2. 入力欄左のマイクボタンを押すと、マイクボタンが赤く光り「聞き取り中...」になります。声で「お疲れ様です」などと喋ってみてください。文字が入力欄に入ります。
3. 送信すると、アバターの横に「入力中...」のドットアニメーションが表示され、少し待つとGeminiからキャラクター別の口調で返事が来ます。

### 💡 3. 予定の自動登録テスト（カレンダー抽出）
1. チャットで「**明日15時に美容院に行くよ**」のように、未来の予定を伝えてみてください。
2. AIが「美容院に行くんだね！予定に入れておいたよ！」などと返し、データベースの `schedules` テーブルに自動でレコード（event_name="美容院", event_date="明日の日付", event_time="15:00"）が保存されるか確認します。
3. ホーム画面に戻ると、予定一覧に「美容院」が追加されています。

### 💡 4. 予定の自動フォロー（問いかけ）テスト
1. （開発中のモック用検証）本日日付の予定（例：「会議」など）をカレンダーに登録します。
2. ホーム画面を開くと、今日の予定に「会議（未確認）」と表示されます。
3. チャット画面に切り替えた際、AIの最初の挨拶が「**今日は会議だったね！どうだった？**」と、本日の予定について自動的に問いかけてくることを確認します。

### 💡 5. 日記の賢い自動要約
1. 会話をいくつか交わした後、チャット画面右上の「**日記をまとめる**」を押します。
2. 日記一覧画面に自動遷移します。
3. 日記カードをタップして開き、会話から客観的な事実と感情が「綺麗な箇条書き」に書き直され、全体の心情が判定されていることを確認します（※おためごかしのない、冷静な要約になります）。
